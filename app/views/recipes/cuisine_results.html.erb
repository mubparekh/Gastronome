<div class="row">
  <div class="cuisine-search-results-box">
    <div class="cuisine-search-results-box-inner">
      <h1>Results</h1>

      <% if @recipes.length > 0 %>
        <table>
          <thead>
            <tr>
              <th>Recipe Name</th>
              <th>Cooking Time</th>
              <th>Rating</th>
            </tr>
          </thead>

          <tbody>
            <%= render partial: "recipe", collection: @recipes %>
          </tbody>
        </table>
      <% else %>
        <span>No Results Found</span>
      <% end %>
      <div class="return-links">
        <%= link_to "Back", cuisine_search_path, :method => :get  %> | 
        <%= link_to "Home", root_url, :method => :get  %>
      </div>
    </div>
  </div>
</div>